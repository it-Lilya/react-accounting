// import React from "react";

// export default function List({lists, onRemove}) {
//     return (
//         <ul>
//             {lists.map((paragraph) => (
//                 <input key={paragraph.id}>
//                     {paragraph.name} <button onClick={() => onRemove(paragraph)}>X</button>
//                 </input>
//             ))}
//         </ul>
//     )
// }